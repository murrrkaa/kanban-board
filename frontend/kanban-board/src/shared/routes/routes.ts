export enum RoutesEnum {
  PROJECTS = "/projects",
  BOARDS = "/boards",
  TASKS = "/tasks",
  USERS = "/users",
  PROFILE = "/profile",
  LOGIN = "/login",
  ROLES = "/roles",
  BOARD_ID = "/:boardId",
}
